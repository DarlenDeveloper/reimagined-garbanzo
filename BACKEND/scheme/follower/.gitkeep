# Follower Service Schema
# Manages buyer-vendor follow relationships
# 🔐 BUYER DATA FILTERED BY: auth.user_id → buyer_id

## Tables:
# - follows (follow_id, buyer_id [FK: buyers.buyer_id], vendor_id, followed_at)
# - follower_stats (vendor_id, follower_count, following_count, updated_at)

## Auth Filtering:
# - GET /following → WHERE buyer_id = auth.user_id
# - POST /follow/:vendor_id → INSERT with buyer_id = auth.user_id
# - DELETE /unfollow/:vendor_id → DELETE WHERE buyer_id = auth.user_id

## Features:
# - Follow/unfollow vendors
# - Follower count tracking
# - Following feed generation
# - New follower notifications
# - Follower list for vendors
