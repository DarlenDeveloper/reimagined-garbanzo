# Auth Service Schema
# Manages authentication, sessions, and password management

## Tables:
# - users (user_id, email, password_hash, user_type[buyer/vendor_user/admin], status[active/inactive/suspended], email_verified, phone_verified, created_at, updated_at)
# - sessions (session_id, user_id, token_hash, device_info, ip_address, expires_at, created_at)
# - refresh_tokens (token_id, user_id, token_hash, expires_at, revoked, created_at)
# - password_resets (reset_id, user_id, token_hash, expires_at, used, created_at)
# - verification_codes (code_id, user_id, code_type[email/phone/2fa], code_hash, expires_at, attempts, created_at)

## Features:
# - Email/password authentication
# - OAuth integration (Google, Facebook, Apple)
# - JWT token management
# - Refresh token rotation
# - Password reset flow
# - Email verification
# - Phone verification
# - 2FA (TOTP)
# - Session management
# - Device tracking
