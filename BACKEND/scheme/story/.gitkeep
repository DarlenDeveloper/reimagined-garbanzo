# Story Service Schema
# Manages vendor stories (ephemeral content like Instagram stories)
# 🔐 BUYER VIEWS FILTERED BY: auth.user_id → buyer_id

## Tables:
# - stories (story_id, vendor_id, media_url, media_type[image/video], caption, views, expires_at, created_at)
# - story_views (view_id, story_id, buyer_id [FK: buyers.buyer_id], viewed_at)
# - story_highlights (highlight_id, vendor_id, name, cover_url, story_ids[], created_at)

## Auth Filtering:
# - POST /stories/:id/view → INSERT with buyer_id = auth.user_id
# - Story feed shows viewed status based on auth.user_id
# - View tracking per authenticated buyer

## Features:
# - 24-hour expiration (free tier)
# - 7-day expiration (premium tier)
# - View tracking
# - Story highlights (permanent collections)
# - Media upload (images/videos)
# - Viewer analytics
