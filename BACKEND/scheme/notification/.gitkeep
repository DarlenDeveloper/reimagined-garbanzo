# Notification Service Schema
# Manages in-app notifications
# 🔐 USER DATA FILTERED BY: auth.user_id

## Tables:
# - notifications (notification_id, user_id [FK: auth.users.user_id], user_type[buyer/vendor], type, title, message, data, read_at, created_at)
# - notification_templates (template_id, type, title_template, message_template, channels[])

## Auth Filtering:
# - GET /notifications → WHERE user_id = auth.user_id
# - PUT /notifications/:id/read → UPDATE WHERE user_id = auth.user_id
# - All notification operations scoped to authenticated user

## Notification Types:
# - order_placed, order_accepted, order_rejected, order_shipped, order_delivered
# - payment_received, payout_completed
# - low_stock_alert
# - new_follower
# - new_review
# - new_message
# - promo_alert
# - price_drop
# - bnpl_payment_due

## Features:
# - Real-time notifications
# - Notification history
# - Mark as read
# - Unread count
# - Notification preferences
