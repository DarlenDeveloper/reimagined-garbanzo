# Webhook Service Schema
# Manages incoming and outgoing webhooks for external integrations

## Tables:
# - webhook_endpoints (endpoint_id, vendor_id, url, events[], secret, status[active/inactive], created_at)
# - webhook_deliveries (delivery_id, endpoint_id, event_type, payload, response_code, response_body, attempts, status[pending/delivered/failed], created_at)
# - incoming_webhooks (id, source[uber/skynet/chipper], event_type, payload, processed, created_at)

## Outgoing Events:
# - order.created, order.updated, order.cancelled
# - payment.received, payment.refunded
# - inventory.low_stock
# - delivery.status_changed

## Incoming Webhooks:
# - Uber delivery status updates
# - Skynet shipping updates
# - Chipper Cash payment confirmations
