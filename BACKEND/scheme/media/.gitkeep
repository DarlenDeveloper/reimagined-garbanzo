# Media & Storage Service Schema
# Manages file uploads, image processing, and S3 integration

## Tables:
# - media_files (file_id, owner_id, owner_type[vendor/buyer/product/post], file_type[image/video/document], original_url, thumbnail_url, cdn_url, size_bytes, mime_type, metadata, created_at)
# - upload_sessions (session_id, owner_id, status[pending/completed/failed], file_count, created_at, expires_at)

## Features:
# - AWS S3 integration
# - Image resizing/optimization
# - Video transcoding
# - CDN distribution
# - Secure signed URLs
# - Product images (multiple per product)
# - Vendor logos/banners
# - Social post media
# - Chat attachments
