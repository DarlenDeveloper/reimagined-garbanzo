# Receipt Service Schema
# Manages digital receipts and purchase records
# 🔐 BUYER DATA FILTERED BY: auth.user_id → buyer_id

## Tables:
# - receipts (receipt_id, order_id, buyer_id [FK: buyers.buyer_id], vendor_id, receipt_number, items[], subtotal, tax, delivery_fee, discount, total, currency, payment_method, pdf_url, created_at)
# - receipt_emails (id, receipt_id, email, sent_at, status)

## Auth Filtering:
# - GET /receipts → WHERE buyer_id = auth.user_id
# - GET /receipts/:id → WHERE buyer_id = auth.user_id
# - All receipt operations scoped to authenticated buyer

## Features:
# - Digital receipt generation
# - PDF receipt download
# - Email receipt delivery
# - Receipt history (shown in profile)
# - Tax breakdown
# - Payment method details
