# Audit Log Service Schema
# Tracks all significant actions for compliance and debugging
# 🔐 USER DATA FILTERED BY: auth.user_id (for login history)

## Tables:
# - audit_logs (log_id, actor_id [FK: auth.users.user_id], actor_type[buyer/vendor/admin/system], action, resource_type, resource_id, old_value, new_value, ip_address, user_agent, created_at)
# - login_history (id, user_id [FK: auth.users.user_id], user_type, ip_address, device_info, location, status[success/failed], created_at)

## Auth Filtering:
# - GET /login-history → WHERE user_id = auth.user_id
# - Audit logs: Admin access only, filtered by actor_id for user-specific queries

## Tracked Actions:
# - User authentication (login, logout, password change)
# - Order state changes
# - Payment transactions
# - Product modifications
# - Inventory updates
# - Permission changes
# - Settings modifications
# - Data exports
